#include <stdio.h>

int main() {
    double side, area;

    printf("Enter the side length of the square: ");
    scanf("%lf", &side);

    // Calculate the area
    area = side * side;

    printf("The area of the square is: %.2lf\n", area);

    return 0;
}
